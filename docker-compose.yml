version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: rena-postgres
    environment:
      POSTGRES_USER: rena_user
      POSTGRES_PASSWORD: rena_password
      POSTGRES_DB: rena_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - rena-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rena_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: rena-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - rena-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:

networks:
  rena-network:
    driver: bridge
